---
title: Hello World
date: 2026-03-29
description: A first post to test things out.
---

This is a sample blog post to verify the setup is working. Feel free to delete this once you've written your first real post.

## Code Highlighting

Here's an example of syntax-highlighted code:

```javascript
function greet(name) {
  return `Hello, ${name}!`;
}

console.log(greet("world"));
```

And some Python:

```python
def fibonacci(n):
    a, b = 0, 1
    for _ in range(n):
        yield a
        a, b = b, a + b

print(list(fibonacci(10)))
```

## Formatting

You can use **bold**, *italic*, and `inline code`. You can also use:

- Unordered lists
- Like this one

> And blockquotes for emphasis.
